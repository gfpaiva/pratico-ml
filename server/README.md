# Server Side

##### Made with:

- Node 9.3.0
- Express
- Axios

##### Instruction to run:

- Open the terminal
- `cd server`
- `npm i` or `yarn`
- `npm start` or `yarn start`

Server will run on port 3001

##### Routes:

- `/api/itesm?q=query`
- `/api/items/:id`
